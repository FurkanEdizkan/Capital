{
  "name": "capital",
  "private": true,
  "description": "Repo-level tooling (husky + commitlint). Apps live in engine/ and web/.",
  "scripts": {
    "prepare": "husky",
    "skills:check": "node scripts/skills-sync.mjs check",
    "skills:update": "node scripts/skills-sync.mjs update"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
