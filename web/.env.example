# Capital web — environment template. Copy to `.env.local` for local overrides.
# Vite only exposes vars prefixed with VITE_ to the browser.

# Base URL of the engine API. In dev this is proxied (see vite.config.ts),
# so the default empty value (same-origin) is correct for `npm run dev`.
VITE_API_BASE_URL=
